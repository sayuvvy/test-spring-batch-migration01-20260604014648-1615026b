package com.migration.batch.config;

import org.springframework.batch.core.configuration.annotation.DefaultBatchConfiguration;
import org.springframework.context.annotation.Configuration;

@Configuration
public class CommonBeanConfig extends DefaultBatchConfiguration {
    // Common beans can be defined here if required
}
